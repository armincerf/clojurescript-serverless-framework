const defaultTheme = require("tailwindcss/defaultTheme");

module.exports = {
  purge: [],
  darkMode: "media", // or 'media' or 'class'
  theme: {
    colors: {
      primary: "#5c6ac4",
      secondary: "#ecc94b",
    },
    extend: {},
  },
  variants: {},
  plugins: [],
};
